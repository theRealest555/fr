// Let's create our environment configuration first to set up API URLs
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5268/api', // Matches the backend URL
  appTitle: 'TE Project'
};
